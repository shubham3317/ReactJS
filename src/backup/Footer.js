import React, { Component } from "react";

/* export default class Footer extends Component {
  render() {
    return <div>{this.props.footerMain}</div>;
  }
} */

const Footer = (props) => {
  return (
    < div > {props.footerMain}</div >
  );
};

export default Footer
